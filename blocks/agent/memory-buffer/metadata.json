{
  "name": "memory-buffer",
  "category": "agent",
  "description": "Sliding context window memory manager for AI agents that tracks conversation history, estimates token usage, and supports summarization when the context limit is approached.",
  "dependencies": [],
  "aiPromptContext": "Use this block when building AI agents that need to manage a conversation history within a fixed token budget. It handles automatic eviction of old messages (sliding window), provides a summarization hook to compress history, and supports serialization for persistence between sessions. Ideal for chat agents, multi-turn LLM pipelines, or any system where context length must be controlled programmatically.",
  "parameters": [
    {
      "name": "maxTokens",
      "type": "number",
      "required": false,
      "description": "Maximum token budget for the context window. Defaults to 4000."
    },
    {
      "name": "tokenizer",
      "type": "function",
      "required": false,
      "description": "Function that takes a string and returns an estimated token count. Defaults to defaultTokenizer (Math.ceil(text.length / 4))."
    }
  ]
}
