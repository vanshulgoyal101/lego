{
  "name": "cron-parser",
  "category": "validation",
  "description": "Standard 5-field crontab pattern parser to validate schedules and resolve subsequent matching execution timestamps.",
  "dependencies": [],
  "aiPromptContext": "Use this block to parse standard crontab patterns ('* * * * *') and compute the next scheduled execution times.",
  "parameters": [
    {
      "name": "cronString",
      "type": "string",
      "required": true,
      "description": "The crontab configuration pattern (5 fields: minute hour dayOfMonth month dayOfWeek)."
    }
  ]
}
