{
  "name": "mime-detector",
  "category": "validation",
  "description": "Magic bytes file signature MIME type detector resolving common image, audio, document, archive, and text structured formats.",
  "dependencies": [],
  "aiPromptContext": "Use this block to guess the MIME type content classification of raw byte arrays/buffers using magic bytes.",
  "parameters": [
    {
      "name": "buffer",
      "type": "Uint8Array",
      "required": true,
      "description": "The raw bytes buffer of the file."
    }
  ]
}
