{
  "name": "rate-limiter",
  "category": "validation",
  "description": "In-memory sliding-window Rate Limiter to protect API gateways or inputs against spamming.",
  "dependencies": [],
  "aiPromptContext": "Use this block inside servers, mock environments, web socket controllers, or serverless functions to rate-limit user actions (IP verification or token limits) without spinning up Redis databases. Import using: import { RateLimiter } from './validation/rate-limiter.js';",
  "parameters": [
    {
      "name": "limit",
      "type": "number",
      "required": true,
      "description": "Maximum actions allowed in window period."
    },
    {
      "name": "windowMs",
      "type": "number",
      "required": true,
      "description": "Sliding timeframe window in milliseconds."
    }
  ]
}
