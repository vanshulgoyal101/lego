{
  "name": "url-template",
  "category": "web",
  "description": "An RFC 6570 Uri Template processor that expands templates using dynamic variables maps.",
  "dependencies": [],
  "aiPromptContext": "Use this block to compile URI template definitions (e.g. '/users/{id}{?query}') into standard formatted URLs dynamically.",
  "parameters": [
    {
      "name": "template",
      "type": "string",
      "required": true,
      "description": "RFC 6570 compliant URI template string."
    }
  ]
}
